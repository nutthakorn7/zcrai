#!/usr/bin/expect -f

set timeout 120
set pass "#8#J8YMULd6u8iQ"
set host "root@45.118.132.160"

# Run dashboard test only and capture full output
spawn ssh -o StrictHostKeyChecking=no $host "cd /root/e2e_runners/e2e && BASE_URL=https://app.zcr.ai ./node_modules/.bin/playwright test 'tests/accessibility.spec.ts:16:7' --reporter=line"
expect {
    "password:" { send "$pass\r"; exp_continue }
    eof
}
