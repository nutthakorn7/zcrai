#!/usr/bin/expect -f

set timeout 180
set pass "#8#J8YMULd6u8iQ"
set host "root@45.118.132.160"

# Run dashboard test with very verbose output to see ALL violations
spawn ssh -o StrictHostKeyChecking=no $host "cd /root/e2e_runners/e2e && BASE_URL=https://app.zcr.ai ./node_modules/.bin/playwright test 'tests/accessibility.spec.ts:16' --reporter=list --output=/tmp/test-output 2>&1"
expect {
    "password:" { send "$pass\r"; exp_continue }
    eof
}
