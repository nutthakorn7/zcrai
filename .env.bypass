# ⚠️ BYPASS AUTH CONFIGURATION
# WARNING: Setting BYPASS_AUTH=true disables ALL authentication!
# Only use for DEMO or TESTING purposes!

# Backend Auth Bypass (for app.zcr.ai production)
BYPASS_AUTH=true

# Frontend Auth Bypass (optional - frontend can detect from backend)
VITE_BYPASS_AUTH=true
